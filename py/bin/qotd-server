#!/usr/bin/env python3

import tcp.server
import qotd.rfc865

handler = qotd.rfc865.Handler()
server = tcp.server.ThreadedServer(9999, handler.handle)

server.start()

try:
    server.run()
except KeyboardInterrupt:
    print("SIGINT received.")
    server.shutdown()

print("Shutting down.")
