#!/usr/bin/env python3

import tcp.server
from qotd.rfc865 import RFC865

server = RFC865(tcp.server.ThreadedServer, 9999)

server.start()

try:
    server.run()
except KeyboardInterrupt:
    print("SIGINT received.")
    server.shutdown()

print("Shutting down.")
